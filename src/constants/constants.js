const BASE_PATH = '/'
const ONE_PAGE = '/one'
const LOGIN_PATH = '/login'
const LOGOUT_PATH = '/logout'

const NOT_FOUND_CODE = 404
const NOT_FOUND_STR = 'URL no encontrada'

const PRIVATE_KEY = 'private_key'
const PUBLIC_KEY = 'public_key'

const EC_CURVE = 'P-521'
const EC_ALGORITHM = 'ES512'

const JWT = 'jwt'
const POOL_CONNECTION = 'pool'

module.exports = {
  BASE_PATH,
  ONE_PAGE,
  LOGIN_PATH,
  LOGOUT_PATH,
  NOT_FOUND_CODE,
  NOT_FOUND_STR,
  PRIVATE_KEY,
  PUBLIC_KEY,
  EC_CURVE,
  EC_ALGORITHM,
  JWT,
  POOL_CONNECTION
}
